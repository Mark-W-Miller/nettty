# Database Morph collection — INCOMPLETE

MPK 0.1.11 includes **no exported database Morph definitions**. Zero included records does not mean
an empty authoritative database. Source identity, consistent snapshot time, accepted revision inventory,
source totals and dependency closure have not been supplied as an accepted export to this release.
See [status](status.json) and [collection contract](../contracts/morph-catalog.md).

The goal remains ALL current accepted Morph identities at exact revisions, with required motions,
materials/media, dependencies, provenance and browsable inventory. Historical revisions are required
only when part of dependency closure. No silent subsets or fabricated records are permitted.

This documentation release is useful without the export and makes no completeness claim. Slice/Tassy
remain source contacts; actual export/validation and subsequent packaging are separate future work.
