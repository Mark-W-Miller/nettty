# Derek, Duke of Bronomesca — two-week campaign advance

Illustrative semantic scenario only: no accepted NPC record, actual dice roll, simulation run or database
write is supplied. Place names/IDs below are author-defined fixtures, not claimed existing catalog data.

## Dormant input

Derek is one stable Character/Morph instance, currently a semantic presence in Bronomesca. His saved
instance-state revision pins a Morph definition and campaign capability profile; it records last resolved
time, typed travel disposition, admitted interests/relationships concerning named people/Places,
unresolved commitments and a next-decision condition. No process runs while he is dormant.

## GM: Advance time by two weeks

- The GM admits day 0 through day 14 under a pinned campaign/world revision, rule set, seed/draw policy
  and hard world-tick/daily account spending limits. Derek is eligible when his next decision falls
  within the range and relevance/proximity/importance policy selects him; most other actors sleep.
- Reserve one call's worst-case cost. Build Derek's smallest typed packet: exact identity/revisions,
  Morph/Place state, travel intention, recent material events, permitted destinations/actions and budget.
  Put it after the byte-stable rules/schema prefix. Make one direct singular API decision call.
- Validate the structured destination/Intention proposal against allowed knowledge, routes, resources,
  commitments and permissions. A forbidden or unknown destination cannot become available just because
  Derek likes travel. Rejected or stale output creates no world effect, but still has a spending record.
- A declared rule chooses among admitted proposals, using recorded dice if applicable. A possible
  accepted result is visiting a named acquaintance in an admitted destination Place. This example does
  not fix a fictional roll as evidence. No viable route yields an explicit deferred/failed intention.
- Between decision calls, the deterministic controller resolves a Place/navigation-pinned route. At distant scale, travel is resolved as coarse
  departure, journey and arrival events, with game-system-owned encounters/social outcomes. It does not
  generate fourteen days of hidden frames. If Derek enters the player's detailed scene, materialize the
  same instance and bind its Path plus distance-driven walk Motion without replaying committed travel.
- Commit the accepted state, relationships/commitments, logical time/frontier, next-decision eligibility,
  audit receipt and readable summary atomically against expected revisions. If the time/work budget
  ends before arrival, retain en-route presence/progress and outstanding events rather than invent arrival.
- Schedule the next decision as campaign due-state, not an automatic real-time background job. A later
  GM-authorized heartbeat selects Derek again when eligible.

## Possible summary shape

“Day 0–14: Derek was selected when due. He chose an admitted destination because of his travel interest
and an existing relationship. Travel and encounters were resolved at coarse event level. His resulting
Place/presence and next decision are recorded in the committed receipt.”

The actual summary must name actual accepted outcomes or partial progress, time range and unresolved
work; it must not use this placeholder as a fabricated history. A map marker, city presence and detailed
seated/walking body all project the SAME resulting actor identity/state. The reusable Morph definition
is not revised merely because Derek travelled or adopted a temporary pose.

## Required proof later

Verify one-call bounded decision dispatch, typed validation, durable accepted state and deterministic
execution between decisions. Replay uses the recorded AI proposal and random draws without a new API
call. Prove coarse-to-detailed consistency, atomic conflict rejection, declared undo limits, next-due state
and identity-preserving materialization. Rendering/physics must make zero AI calls. No such execution,
provider usage, spending or cache-hit measurement is claimed in this Kit.

## Budget and failure variants to verify

- Record total/ordinary uncached input, cache writes, cached reads, output, model, latency, price version
  and cost for Derek's call, then aggregate with every attempt in the heartbeat. Missing usage remains
  pending with its reservation held. A stable prefix alone is not evidence of a hit or a free call.
- If the next call cannot fit both tick and daily limits, Derek stays deferred or continues an already
  admitted safe plan. Advancing two weeks does not reset the budget for each selected actor/substep.
- If the world changes during inference, reject the stale commit. Preserve the paid-attempt record;
  any new decision needs fresh admission and reservation. Undo cannot refund the provider charge.
- Prompt caching reuses prefix computation; saved Derek state supplies memory. No retained conversation
  or fourteen-day transcript is needed. A later due decision builds a new minimal snapshot.
