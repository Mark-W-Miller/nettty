# What's new

## 0.1.13 — 2026-09-24

- Defines local project servers as the database-capable development workshops, with direct supported
  Save for selected database objects and retained disk/offline authoring.
- Makes Tassy the assembled rehearsal of Moonbeam: both delivery profiles serve a read-only canonical
  SQL baseline while editors retain edits and new records in browser-owned overlays.
- Uses runtime capabilities rather than hostnames or ports for authority and keeps component publication
  and content-baseline publication as separate receipt streams.
- Separates authored browser data from disposable cache and adds origin separation, quota/export recovery,
  baseline conflict, multi-tab and optional LAN synchronization boundaries.
- Replaces the one-line Context summary with self-contained typed envelopes and distinct Motion and
  Citadel-building profiles, including exact revisions/units, resolvable bytes and authorized operations.
- Records the current writable Tassy Slice declaration as a mismatch requiring Slice-first proof; no
  runtime, database, Moonbeam, provider, release or consumer distribution change is claimed.
- Adds a complete browser-workspace/Archive/Reinflate contract with trusted application profiles,
  origin separation, transaction/recovery invariants, malicious-archive rejection and application proof.
- Adds compiled Morph package identity, invalidation, authorized compile-after-read, atomic activation,
  distance-level semantics and honest end-to-end timing.
- Adds press-and-hold radial menu gesture arbitration, persistent palette/command boundaries and
  keyboard/touch acceptance requirements.
- Adds baseline-bound Place package exchange, explicit sparse changes/deletions, stable structural
  identities, conflict recovery and native-admission boundaries.
- Curates one Rougish body, one Slice body and one byte-identical Talisman shared-storage body; records
  retained local material and unresolved runtime/device proof.
- Adds an exact component-publication plan validator and typed owner-adapter boundary. It verifies the
  complete package inventory and validates remote-selection/public-served receipts. Workboard's typed
  call and Tassy's reusable adapter are focused-tested; the concrete machine transport and live receipt
  remain external proof without embedding credentials.

## 0.1.12 — 2026-09-22

- Incorporates Rougish's portable terrain-navigation findings into one shared contract for all 3D views:
  left select/edit, middle-drag pan, right-drag orbit, double-right-click orbit focus and cursor-directed zoom.
- Defines continuous nested Place refinement: stable parent-child registration, bounded progressive detail,
  one composed child surface, child-owned interior geometry and parent-owned seam continuation.
- Preserves cursor focus and camera continuity through explicit approach, inward/outward loading, transition,
  cancellation and failure states; outward parent selection begins at one-third projected viewport span.
- Separates navigation, cache and derived surfaces from authored changes and from browser, disk and canonical
  database authority. Adds focused acceptance scenarios without claiming consumer runtime adoption.
- Includes the active-development-safe repository publisher update made after 0.1.11; unrelated owner work
  and `project-kit-local/` remain outside MPK delivery commits.
- Curates Slice's shared findings for Save versus Import authority, exact recovery receipts, layer
  representation, stable asynchronous selection/loading, displayed-source authoring and consumer closure.
- Adds multiresolution Place/instance rules, motion preview/composition boundaries, asset-byte cache identity,
  endpoint-specific limits and evidence that tests the real structural/display path.

Limitations: Rougish and Slice source/focused evidence support the contracts, but this release does not prove
every scenario, physical-device interaction, full Slice save/import/deploy/reopen or canonical database acceptance.

## 0.1.11 — 2026-09-19

- Defines the release-11 DTDT UI vocabulary for Cards, Shelf Items, Shelves, Boxes, Arenas, Six-View Bars,
  tear-off tool bars and twist-open tree shelves.
- Makes the Control-Tick debug overlay a compliance requirement: every meaningful structural UI element
  must expose a tiny inspection control that opens its DTDT specification.
- Adds the portable database-packet and canonical importer contract for Rogish (Rougish in older kit notes), Insect Wars,
  Slice, Dwarf War / Dwarven Warfare Simulator and Moondance/Talisman tools. Projects may operate in
  patch-only/chat, local-runtime or direct-database modes, but their Morph/Place packets must remain
  database-shaped and importable.
- Defines the shared canonical database importer as a controlled local-area/system-administrator screen,
  preferably hosted by the Talisman Application Server, with project registration, side-by-side compare
  and explicit import/update/skip/merge/quarantine/reject dispositions.
- Formalizes the Theme contract and Talisman Forest family variants Forest Green and Forest Blue, using
  the same semantic roles rather than per-screen colors.

Limitations: this is documentation/contract work. It does not implement the importer screen, project
adapters, Control-Tick runtime overlay, database writes or consumer UI conversions.

## 0.1.10 — 2026-09-18

- Adds authored-only speech, typed recipes, separate language/performance, inheritance and cache rules.
- Adds application-owned voice-assistance history, relative proposal refinement and explicit acceptance.
- Retains our hard-coded provider options; configurable local fields are not promoted.
- Records source hashes, local-only details, deferred performers and unverified audible/browser gates.
- Includes the scoped consumer commit/push script, its instructions and focused tests.

## 0.1.9 — 2026-09-18

- Adds VERSION-0.1.9.md at the Kit root so the installed version is visible in directory listings.
- Includes the marker in the tagged inventory; installation removes obsolete shared files/markers.
- Preserves project-kit-local and root project guidance. Delivery receipts remain separate from
  consumer commits, runtime implementation and acceptance.

## 0.1.8 — 2026-09-18

- Adds Voice Lab documentation, package schema/template/example and page-level speak/stop integration.
- Collects Rougish's modeless AI dialogue, context inspector and explicit candidate acceptance pattern.
- Fixes all AI provider/model/endpoint options to choices hard-coded by us; no user-added providers.
- Adds pinned-owner collection with exact working-file snapshots, handoff receipts and policy-gap reports.
- Adds published-tag installation into project directories, verified inventories, previous-copy backups,
  exact replacement of obsolete Kit files and per-project delivery reports. No consumer commit/push.
- Supplies focused collector/installer tests and commands suitable for later administrator buttons.
- Preserves prior releases and local authority. No voice engine, provider runtime, database operation or
  consumer runtime acceptance is supplied or inferred from documentation installation.

## 0.1.7 — 2026-09-17

- Adds freely authored/exchanged Morph and Metamorph behavior modules, primarily JavaScript for
  browser/Tassy, with exact version/digest bindings and typed configuration/events/state.
- Specifies registry/manifest, lifecycle hooks, ordering/composition, provenance, deterministic/replay
  declarations, capability-scoped execution and trusted built-in versus imported/untrusted tiers.
- Requires enforced time/memory limits, cancellation, audited host-approved capability requests and no
  ambient filesystem/network/database/secret access; import/storage does not execute a module.
- Adds a small material-pulse example; preserves AI scheduler/spending and game/physics authority.
- Executor request arrived after 0.1.6 publication; preserves that release and all prior artifacts.
  No module runtime, sandbox, consumer adoption or database change is implemented or claimed.

## 0.1.6 — 2026-09-17

- Corrects 0.1.5's optional-edge/GPT-independent framing: AI is deliberately built into bounded runtime
  decisions; deterministic Morph/Place/game machinery executes accepted state between calls.
- Specifies sleeping/eligible actors, budgeted campaign scheduling, minimal typed snapshots, one direct
  API call per decision, proposal validation and durable atomic accepted state.
- Requires byte-stable rules/schema prefixes and dynamic state tails; distinguishes prompt caching from
  retained conversation context, with measured cache reads/writes, tokens, latency, model and cost.
- Requires atomic conservative reservations and hard world-tick/daily world/account spending limits;
  failed/stale calls still count. Forbids inference on render/physics ticks and during replay.
- Reconciles campaign/behavior/physics, Derek, architecture/status and migration documentation; one
  repository is the adoption unit, shared by its branches.
- Preserves prior release artifacts and historical notes; the 0.1.5 optional-only AI statement below is
  superseded. No consumer adoption, provider call/spend, runtime implementation or database work claimed.

## 0.1.5 — 2026-09-17

- Extends typed Intentions to dormant campaign NPCs and explicit GM logical-time heartbeats.
- Requires bounded interest-driven event resolution, atomic audited world updates and explicit replay/undo.
- Preserves one instance identity across semantic presence, map marker and detailed body/Path/Motion.
- Requires GPT-independent typed execution; language interpretation remains an optional proposal front end.
- Adds Derek, Duke of Bronomesca’s two-week example, migration and explicit unproven-runtime status.
- Adds optional engine-neutral physical proxies, Place world prerequisites and a future pluggable resolver;
  separates physics contacts from game authority, hybrid transform ownership and replay/LOD limits.
- Preserves 0.1.4 and all prior releases; no consumer upgrade, automation or database work performed.

## 0.1.4 — 2026-09-17

- Defines the shared Talisman Console for Insect Wars, Rougish, Dwarf War and Slice.
- Specifies both upload and database-to-project transfer, coherent dependencies and safe local activation.
- Separates shared catalog storage, origin/domain organization, native types and live actor instances.
- Requires independent console access, guarded identities, explicit transfer plans and read-back.
- Requires precise owner bindings, patcher fixture tests and subsequent local live-server acceptance.
- Strengthens Morph to universal articulated/spatial content: Control Point forests, separate constraints,
  arbitrary geometry, partial Stances, deterministic composed Motions and nested instances.
- Defines Metamorph as the stage/world graph and domain types as projections; unifies Slice/Thinian semantics.
- Adds six semantic examples, source-tree materialization/readback and storage-independent invariants.
- Retains SQLite for local/Tassy; recommends a hybrid logical store and measured PostgreSQL migration path.
- Separates Motion, Path, Intention and game authority; adds controller/interaction examples and
  persistence-neutral lifecycle guidance. Records browser-first Tassy UI as candidate architecture.
- Preserves the pre-existing console draft; no runtime/database changes or cross-project adoption claimed.

## 0.1.3 — 2026-09-16

- Makes project-kit-local upkeep, Patcher return and owner-reviewed handoff to Merlin standard procedure.
- Adds local supplement/upstream feedback forms and review dispositions: promote, retain, defer or reject.
- Curates Dwarf War lessons about explicit offline authoring, hosted authority and app-owned versus
  shared native acceptance; keeps game assignments and publishing rules local.
- Incorporates the formerly separate sanitized Tassy/Slice owner evidence with historical limits.
- Requires exact adoption receipts from all four repositories and preservation of local guidance.
- No runtime/DB changes, native acceptance or completed catalog are claimed.

## 0.1.2 — 2026-09-16

- Adds complete independent offline compendia and one real native definition for local render/edit/save
  and database payloads; appearance/motion-preserving conversion remains implementation work.
- Corrects NEW-only guidance: edit the same ID through guarded UPDATE; clone only deliberately.
- Defines plural Morph-only return ZIP requirements, dependencies, expected guards and chosen policy.
- Requires automatic designated validate/apply/readback orchestration, replay/recovery and activation
  after closure; no manual per-object ceremony or false set-atomicity claim.
- Adds source-inspected NEW/UPDATE behavior and next end-to-end proof through Slice display.
- Three labs adopt before software work; all three plus Slice require hot deployment/database access.
- Requirements and implemented evidence are distinct. Full catalog, codecs/adapters, ZIP importer and
  completed application roundtrip remain unproven; no database writes were performed.

## 0.1.1 — 2026-09-16

- Standard Place minimum: numeric height plus texture aligned in extent, units and axes, with optional
  refining layers. Flat numeric height is allowed; missing data is never fabricated. Top-first layer
  display does not turn height geometry into a color overlay.
- Local-first native Morph reuse, independent derivatives, provenance, validation, supported admission,
  receipts/readback and activation after dependency closure.
- Slice-grounded standard-object direction, with exact codec/example binding explicitly pending.
- Insect Wars lessons: local staging validation missed native stances/keyframes and interpolation rules.
  Native-validated fixtures remain required before claiming content acceptance.
- Sanitized owner-supplied per-item Tassy save contract; no invented universal importer or set atomicity.
- Reusable baseline/preview, content, hosting and acceptance lessons from local supplements.
- Explicit incomplete all-Morph catalog status; updated Place authoring form, migration, contents and manifest.

Limitations: no catalog export, completed native object fixture, actual import/readback or runtime proof.
This documentation release does not wait for those outcomes and does not claim them.

## 0.1.0 — preserved first release

Initial portable architecture documentation, templates, historical Talisman references and contents view.
Tag v0.1.0 and its bytes remain unchanged. The intervening 0.2.0-draft.* archives were unreleased working
previews; they are not newer shared releases than 0.1.1.
