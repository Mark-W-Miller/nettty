# Active Designs lifecycle — manual Patcher handoff

Status: prepared, not delivered or acknowledged. Recipient: existing `__Talisman Architecture`.
Owner: Atlas — Talisman Architecture__, `codex/development-architecture-atlas`.

## Paste with the exact source ZIP

Read and apply AGENTS.md (including Communication with Mark), AGENTS.project.md, the Development
Architecture Atlas, AGENTS.index.md, and
`design/Active Designs/Talisman Architecture/ACTIVE-DESIGN.md`. The latter is the current commission.
Read MOONDANCE-PROJECT-KIT.md and the earlier PROJECT-KIT-HANDOFF.md for retained Kit requirements;
this message replaces the earlier recipient/baseline instructions. Do not follow superseded Journey
ownership or Main-working directions in any draft. Atlas owns review on its permanent branch.

Identify the attached ZIP, SHA-256 and exact source revision evidence before work. It must contain this
lifecycle commission and the landed Project Kit history, not just an older Main archive. Missing identity
or absent controlling files is a concrete handoff gap; do not silently substitute another baseline.

Produce one consolidated documentation-only patch: inventory every existing Active Designs document;
classify current designs, enduring contracts, completed/inactive/history and unresolved material; propose
safe preserved moves outside Active Designs; maintain one ACTIVE-DESIGN.md per genuinely current design;
repair indexes and links; and extend the Project Kit design/template/validator specification to teach and
check the same lifecycle. Use the exact acceptance and preservation rules in ACTIVE-DESIGN.md.

Keep Mark's current live portfolio names Slice, Dara, Mandy, Tassy, Spelunker and Relicary, with technical
identities preserved. Do not fabricate external repository inspection, activity, objectives or owner
confirmation. Include Sally's supplied cleanup only as attributed draft evidence; classify each proposed
change and reject superseded ownership. Complete supported work without asking for per-file reviews.

Preserve original documentation and source history. Return old/new path and preservation manifests,
source baseline identity, current front-door/index map, unresolved decisions, Kit lifecycle validation
fixtures/expected outcomes, changed paths and actual focused checks. Separate proposed tests from checks
run. No executable source/test/tool changes, database or server operations, Kit repository creation, game
migration, Main landing, or other-worker contact. No broad build/test suite. Stop only the dependent part
when an actual missing input or authority decision blocks it, and explain the exact remaining gate.

## Mark's attachment step

After Atlas publishes this preparation and resolves Sally's transfer, create a full source ZIP from the
clean published **Atlas branch tip**, including this instruction and its ACTIVE-DESIGN.md. Record the exact
tip and ZIP SHA-256 with the attachment. Attach it to `__Talisman Architecture` and paste the block above.
A Main-only ZIP currently omits this unlanded commission. Do not describe the Atlas ZIP as Main. No ZIP
or message is created by this preparation; confirmed receipt is still required before reporting started.

## Existing Atlas knowledge and first-return review artifact

Mark confirms the Patcher also has an existing Atlas website and a server he can start. Extend that
existing site; locating its source/open route is part of the Patcher work, not a preparation blocker.
Use `development-atlas/Development-Architecture-Atlas.md`, its companions, AGENTS routes, current designs,
operations evidence and implementation-friction records as the authoritative source foundation. Preserve
that architecture and its useful links. The first return must extend the existing HTML/CSS/JS review view alongside that foundation; it must
not establish a competing architecture source of truth or parallel site.

Every Moondance project, including separate repositories, needs a physically included, self-contained
architecture/context package. The full source ZIP must include the current architectural layout, relevant
contracts, ownership and service boundaries, design decisions, validation routes, agent setup and feedback
records needed to understand and work on it. Inventory coverage explicitly; do not include only the new
Kit proposal or rely on another repository or chat history. Preserve the owner-to-Atlas feedback loop:
record observed friction, return actionable findings to the responsible owner, and retain the resolution
with evidence; a friction report alone does not authorize unrelated changes or reject a landed body.

Explain how the Kit vendors this knowledge and adapts it locally while keeping project-specific authority
and technical identities. Do not copy absent external repositories or claim they were inspected. The
current Talisman architecture provides the reference; the design must specify what each future consumer
carries, updates and validates independently. State the exact local opening route for the returned view.

The integrated default view must explain Kit layers/languages, package shape, Active Designs lifecycle,
ownership/authority, templates/defaults, source-to-database materialization, service/context contracts,
validation, version/lock/migration, Patcher/full-ZIP exchange, adoption order and open decisions. Use
Shelf/theme conventions, meaningful color with text/icons, progressive disclosure, readable relationships
and useful document cross-links. It must open locally without a server or network dependency and show
an exact source baseline and readable build label. Label proposed, implemented, validated and deferred
material distinctly. Never simulate database state, completed work, deployments or live services.

This later commission permits HTML/CSS/JS solely for that Atlas design/review surface in the
returned patch. It does not authorize product executable changes or implementing the Kit. Preparation
here remains Markdown-only. Focused acceptance includes static syntax, local links/assets, keyboard and
focus/disclosure, accessible names, noncolor status, zoom/narrow layout, offline file opening and browser
console/network checks. Record actual browser evidence separately from proposed checks and limitations.
Keep the return on Atlas; no Main publication before Mark reviews it.

## Queue contract in this return

Read PATCHER-QUEUE-CONTRACT.md in the parent design folder and include its declaration template, state
model, evidence/transition rules and fixtures in the Kit design/review view. Preserve the first body ID
`atlas-active-designs-project-kit-001`. Do not implement Sam's queue page or advance delivery state.

## Always-inspectable Kit — Mark's website clarification

The existing Atlas site must expose a detailed Project Kit overview as its first/default entry, with
nested twist-open sections for progressively deeper component detail. For every component show purpose,
contents/files, dependencies and relationships, owner, contract, examples, validation evidence, current
state and remaining work. Include source links and exact snapshot/build identity. Keep useful existing
views. Make the contents view maintainable from the Kit inventory so Mark can inspect what it contains
throughout preparation, construction and eventual distribution; label stale snapshots honestly.

The Patcher identifies and reuses his existing website/server route and returns the relevant source and
simple opening instructions. Preserve a server-free local review option where feasible; if the existing
site requires its server, document that fact and the bounded offline adaptation rather than inventing
an already-working file-open route. The missing local copy does not prevent preparing this assignment.

The intended product direction is a built, distributable self-contained Kit with continuous visibility.
This first return still supplies inventory/design/cleanup and the review surface; repository creation,
release construction and distribution follow the accepted design and their separately commissioned
bodies. Do not label a specification or visual preview as a built or distributed Kit.

## New-project bootstrap authority

Mark assigns new Moondance project creation to the Grand Pubah. Every new project must receive the
approved Moondance Project Kit as a complete physical vendored snapshot, alongside its project-specific
files. The Grand Pubah ensures the front door, architecture/agent context, templates, schemas, validation
assets and required dependencies are present; records the exact release/source/digest and adoption in the
project lock; and obtains repository-local validation evidence before declaring Kit setup complete.
No symlink, sibling checkout or implicit chat context substitutes for the included Kit. Projects retain
their own source, data and runtime authority. If no approved release exists or validation fails, record
Kit setup as pending/blocked with the exact reason; never claim installation or invent a release.
This standing bootstrap requirement is not an instruction to create a project or distribute a Kit now.

## ZIP-only ceremony and root discovery

Mark requires the prepared full-source ZIP to be sufficient on its own. Its root README.md must identify
the exact queued assignment and link directly to the controlling ACTIVE-DESIGN.md, complete instructions,
required reading and return/acceptance criteria. No separate pasted brief or directory hunt is required.
Mark uploads the ZIP and may simply say “find it”; later discussion may refine the work.
Preparation checks root README discovery and all required local targets in a fresh extraction. Include
root PATCHER-SOURCE.json with exact source revision, body/attempt, file inventory and packaging overlays;
record ZIP SHA-256 outside the archive. Do not silently replace an existing project README: preserve its
human navigation and integrate the assignment entry, or declare the reviewed packaging overlay explicitly.
READY_FOR_MARK requires this actual verified package. An instructions-only draft is not a prepared ZIP.
