# Moondance Project Kit — Patcher instruction

Prepared 2026-09-15 for manual paste to the existing `__Architecture Atlas` companion.
Status: retained earlier preparation; use LIFECYCLE-HANDOFF.md for the current restored Atlas commission.
Not sent or acknowledged.
Commission baseline: `2723cd2540918b09b9e0025bc9e535c6c83c9db0`.

## Paste with the ZIP

Examine the attached full Talisman Main source ZIP and complete one documentation-only body:
repository-wide shared-contract inventory and the exact Moondance Project Kit architecture/design.
Return one consolidated reviewable documentation patch at the end; no individual-stage review stops
unless an actual missing input or material authority decision prevents the design.

First identify the supplied archive, its SHA-256 and available exact source revision evidence. Confirm
it contains the controlling `design/Active Designs/Talisman Architecture/MOONDANCE-PROJECT-KIT.md`
commission landed at `2723cd2540918b09b9e0025bc9e535c6c83c9db0`. The attached ZIP is your actual
baseline; do not substitute an older ZIP or invent a revision if archive evidence is absent. Report any
identity gap explicitly. A later clean Main snapshot may include the commission and subsequent work.

Read and apply `AGENTS.md`, including Communication with Mark, then `AGENTS.project.md`,
`development-atlas/Development-Architecture-Atlas.md`, `AGENTS.index.md`, the controlling Project Kit
design, and `design/MOONDANCE-PROJECT-PROFILE.md`. Follow their relevant action, service, threading,
persistence, focused-test and subsystem routes. Inspect source, schemas, fixtures and nearest package
contracts to distinguish landed behavior from proposed design. Treat source-archive execution limits
honestly; never claim a configured test ran when its toolchain was unavailable.

Produce these connected deliverables:

1. **Inventory and authority map.** Cover DTD/DTDT, native Morph, Shelf, Context question/answer,
   services/process/database ownership, themes/accessibility, templates/defaults, provenance/revisions,
   validation and agent/repository setup. For each candidate cite exact repository paths, owner,
   current status, dependencies, portability decision and destination. Separate portable base,
   project-specific adapter/content, historical material and missing evidence. Inventory the supplied
   repository fully; do not claim to inspect absent Slice, Dwarf War or third-game repositories.
2. **Exact proposed Kit tree.** Resolve file formats, names, ownership and extension boundaries for
   the front door, standards, schemas, Place/Character templates, defaults, fixtures, validators,
   migrations, themes, tools and docs. Design a complete physically vendored snapshot in each game,
   independently understandable and validatable without Talisman, symlinks, submodules, sibling paths
   or package-only missing contracts. Include portable asset licensing and dependency closure.
3. **Release and adoption contracts.** Specify manifest and consumer lock schemas and examples,
   canonical inventory/digest algorithm (paths, bytes, exclusions and normalization), semantic
   versioning, support windows, source revision/date, adoption time, stable extension identities and
   compatibility states from the commission. Separate recorded validation evidence from version
   selection and newer-release availability; offline knowledge must not imply current remote truth.
4. **Complete template contracts.** Use required components + documented defaults + validation rules
   + extension points. Cover every Place and Character field in the controlling design. Specify
   concrete safe defaults, hint precedence, units, relationships, stable identities, revision rules,
   unresolved references and admission failures. Keep game rules out of the canonical base. Preserve
   complete character sheets, Morph/appearance/stance/motion, inventory/equipment, location,
   visibility/ownership and provenance without inventing support in current Talisman APIs.
5. **Source-authoritative materialization and updates.** Design deterministic identity-stable import,
   preview, version-aware migration and exact receipts. Separate canonical base, extensions and
   user-authored data. Specify typed intent, UI-independent adapter, owning transaction authority,
   immutable result, stale/replay/cancellation guards and applicable worker/UI boundaries. One checked
   database transaction must commit all admitted changes or leave the database unchanged; a writer
   lease or sequence of independently committed calls is not atomicity. Identify current API gaps,
   artifact staging/crash recovery, idempotent reinstall, rollback and conflict preservation. Never
   silently overwrite user edits, extensions or accepted repository state.
6. **Validation and staged adoption.** Map all thirteen checks in section 7 to deterministic fixtures,
   expected pass/fail evidence and proposed validator entry points. Cover clean bootstrap, hints,
   extensions, digest mismatch, stale locks, divergence, missing migrations, database rollback and
   full-source ZIP self-containment. Plan gates in order: accepted design, dedicated Kit owner/repo,
   new third game, Dwarf War, Talisman, Slice, then Workboard visibility. Name unresolved decisions,
   missing inputs and acceptance owners; none of these future stages starts in this body.

Preserve the controlling scope. Do not create a repository, migrate games, modify executable source,
implement validators/tools, access or alter databases, change servers/deployments, implement Workboard,
contact other owners or infer authority for later steps. Schemas, examples and proposed commands may
appear as documentation code blocks; do not install them as executable or runtime assets.

Return one documentation patch against the exact attached baseline, with a readable summary, changed
paths, inventory coverage, explicit decisions/gaps and focused proof. Keep output under the existing
Talisman Architecture active-design area, update its living TEST-PLAN and relevant design navigation,
and preserve the controlling commission. Check local links, added-line lengths, whitespace and the
changed-path boundary. Report every check actually run and any environment limitation; do not widen
to a build or broad suite. Separate the proposed Kit validation plan from verification performed now.
