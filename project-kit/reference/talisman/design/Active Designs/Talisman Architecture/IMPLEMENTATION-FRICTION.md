# Implementation friction reports

Reports are owner-supplied evidence tied to exact bodies. They do not independently prove landing,
runtime availability, or verification, and do not authorize changes in another owner's implementation.

## Morph Patch Chat — c39735cbe3936af79c5f393cfd6af1c8047d3639

Reported by Morph Patch Chat on 2026-09-05.

- Worked well: source-authoritative generator, immutable Core Morph revision catalog, typed
  CoreMorphCatalogService, generated response fixtures, Shelf v2 contract, and Atlas HTML-to-DTDT/Java
  seam made additive materialization and UI ownership boundaries direct.
- Product friction: the database geometry guard assumed membrane-only attachments. Complete Humanoid
  capsule geometry required a narrow extension for exact parent-child two-Point capsules.
- Delivery friction: the cumulative external patch targeted a pre-catalog baseline and required
  reconciliation rather than direct application. Preserve exact source baseline in future handoffs.
- Owner reports Morph design/current Atlas updates and passing focused Atlas validation. This receiving
  task has not independently verified that commit, test result, landing state, or runtime state.
- Disposition: catalogued; no cross-owner redesign or new implementation assignment. Watch for repeated
  geometry-guard constraints before proposing a broader architecture change.

## Morph Patch Chat — 8566b8846a0abf7a977b398d0413fd8b20126ffb

Reported by Morph Patch Chat on 2026-09-05.

- Worked well: canonical Shelf Contract v2 validation and stable data-of-* conversion identities made
  owner DTDT, declared policies, and DOM inventory straightforward to validate. Core Morph validation
  cheaply proved the twelve authoritative Morph packages remained unchanged.
- Testing friction: the supplied patch required isolated Chromium interaction proof without providing
  a reusable checked-in harness. Disclosure, focus, divider, memory, and no-network checks therefore
  required a one-off headless Chromium run.
- Owner reports updates to development-atlas/service-architecture.md and
  development-atlas/focused-tests.md, with passing Atlas validation. Receipt of this report does not
  independently verify those claims, Main landing, or runtime state.
- Disposition: reusable browser interaction evidence is a candidate improvement for the owning task.
  No harness implementation or cross-owner change is assigned by this report.

## Morph Patch Chat — c516cce7a7b3de71750c35f8646b5234bed400b3

Reported as published by Morph Patch Chat on 2026-09-05; publication was not independently checked here.

- Worked well: canonical Shelf v2 validation, owner-definition/DOM checks, stable data-of-* projection
  seams, and complete Core Morph validation made authority and byte-preservation proof direct.
- Delivery friction: the cumulative patch used older baseline c39735cbe and required three-way
  reconciliation with the already-landed v2 body for its Point-parameters/Puppeteer intent.
- Interaction finding: real interaction testing exposed a stale v1 HTML fingerprint and focus-induced
  reappend of unchanged Shelf DOM causing first-click disclosure failure.
- Recurring testing friction: no checked-in reusable browser interaction harness exists for this
  workbench. Final pointer, keyboard, navigation, and influence proof required an isolated one-off
  Playwright run. This repeats the harness gap reported for 8566b8846a0abf7a977b398d0413fd8b20126ffb.
- Owner reports no Java service, database, provider, production bus, or server lifecycle boundary change.
- Disposition: evidence now supports proposing an owner-scoped reusable interaction harness covering
  first-click disclosure, retained DOM identity across focus, pointer/keyboard navigation, and influence
  controls. This intake does not assign that implementation or independently certify the reported proof.

## Morph Patch Chat — 3280141f6d96a3783e91652c4c355e17028a0454

Reported by Morph Patch Chat on 2026-09-05.

- Worked well: browser-local presentation-only camera ownership and existing stage-owned pointer
  capture allowed a narrow event-priority correction without service, bus, persistence, or Morph
  authority changes.
- Interaction friction: the orbit widget is recreated on every camera render. Initial pointer
  ownership must resolve from its explicit hover target; drag continuity must remain captured by the
  stable stage rather than the disposable widget node.
- Owner reports focused Chromium proof of hover feedback, widget Shift-drag orbit precedence,
  preserved empty-stage pan/orbit gestures, and zero console/page failures. This intake does not
  independently verify those results or establish publication, landing, or runtime state.
- Disposition: retain as concrete event-ownership guidance and a candidate scenario for any future
  owner-scoped browser interaction harness. No new implementation is assigned.

## Morph Patch Chat — 40dd3f965bc09a7ec3104da9bc06109acf8d4964

Reported by Morph Patch Chat on 2026-09-05 as on Main; landing was not independently checked here.

- Worked well: the authoritative Core Morph resource builder and typed catalog/read tests made
  source-to-database materialization explicit and verifiable.
- Recurring delivery friction: the cumulative external patch preceded newer Shelf and orbit fixes.
  Reconciliation required preserving Shelf reordering, fingerprint and memory behavior, and orbit-widget
  pointer priority. This continues the earlier stale-baseline pattern.
- Domain/test change: the authoritative Ancient Dragon design replaces six flat geometry attachments
  with two first-class Web objects; the old geometry expectation therefore required revision.
- Reported authority boundary: complete Core Morph packages own geometry/Webs. Database rows are
  deterministic materialization. Workbench navigation, gizmos, influence nodes, and previews remain
  browser-local and introduce no second persistence route.
- Disposition: retain exact-baseline handoff and domain-aware test expectation changes as evidence.
  No cross-owner implementation is assigned, and this intake does not independently verify runtime state.

## Morph Patch Chat — 964c7022dd8968a7378303533f6cbd054f44c3ba

Corrective-body report received from Morph Patch Chat on 2026-09-05.

- Worked well: CoreMorphCatalogService immutable-revision enforcement detected changed package bytes
  reusing an existing revision identity. The deterministic builder produced additive successors.
- Diagnostic friction: the first startup failure named only Serpentine, but the generator had rewritten
  all twelve current packages. A full catalog comparison was necessary to establish the actual scope.
- Owner-reported correction: preserve every historical package byte, advance catalog revision to 4,
  and select twelve additive successors. No database edit or server lifecycle action was reported.
- Disposition: record the successful integrity guard and the need to compare all generator outputs after
  an immutable-identity failure. This is evidence for focused catalog-wide byte-preservation coverage,
  not authorization for a broad test suite or a new cross-owner implementation.
- Verification, publication, landing, and runtime recovery were not independently checked by this intake.
