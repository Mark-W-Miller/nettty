# Adopt MPK 0.1.4

Documentation update from 0.1.3: implement the [shared Talisman Console](../contracts/talisman-console.md).
Read alongside the existing Morph return-cycle contract. Preserve earlier releases, project root
instructions, source/runtime identities, local supplements and user checkpoints. Record exact installed
version/digest and owner acceptance; do not present distribution as consumer adoption.

All four consumers—Insect Wars, Rougish, Dwarf War Combat Simulator and Slice—use the same bidirectional
transfer semantics and independent console access. Reconcile existing integrations rather than replacing
them blindly. Local-host pairing is not the Talisman connection. Keep game-specific layout and lifecycle
details local; Insect Wars explicitly requires three independent rails and locally served operation.

Before implementation, complete the service binding sheet in the contract with actual owner operations.
Patcher fixtures prove client behavior only; project owners perform subsequent real-server checks.
Catalog completeness, native codecs, kind-specific adapters and live transfer acceptance remain separate
gates. No application-server/database changes or consumer adoption are performed by this release.

## Reconcile the strengthened Morph language

1. Preserve the console work in this release. Read Morph, Metamorph, persistence and capability-status
   documents before interpreting historical creature codecs as universal requirements.
2. Audit editor/codec coverage for disconnected Point forests, arbitrary local geometry, constraint
   graphs, partial Stances, motion composition, nested instances and stage clocks. Record precise gaps.
3. Do not bulk-convert saved records or invent compatible-looking JSON. Plan owner-approved semantic
   adapters preserving IDs/revisions/assets/edges and behavior; compare source/target round-trips.
4. Place/Character/object classifications remain typed views. Preserve existing queries and authorities
   while bringing geometry/motion under the same language. Slice and Thinian are complementary editors.
5. Retain core/default native packages in source; document explicit materialization, guards, receipts,
   readback and source-versus-user-edit authority. Offline preview alone does not admit content.
6. Keep SQLite locally/on Tassy. Benchmark actual contention, multi-server needs and graph latency before
   any PostgreSQL migration; storage work is separately authorized, not performed by Kit adoption.
7. The six semantic examples are not accepted native records. Prove their codec/evaluator/round-trip
   behavior in a later implementation pass. Adopt the exact new Kit deliberately and return receipts.

## Behavior and UI boundary

Separate local Motion from Place-bound Path and admitted Intention. Add controller bindings and explicit
game-system authority, lifecycle/replanning/cancellation outcomes; do not revise reusable Morphs for
instance journeys. Authored behavior/checkpoints need identity/provenance and restore-time checks.
Treat browser-first Tassy as candidate direction only: inventory/replace native functions and verify
any migration plus browser acceptance before claiming Java UI removal.
