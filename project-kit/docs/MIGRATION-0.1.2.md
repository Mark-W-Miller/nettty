# Migrating 0.1.1 to 0.1.2

Adopt in Dwarf War, Insect Wars and Rougish BEFORE further software work. Slice receives the collector/
visual-proof guidance as the fourth consumer. Preserve root ownership, local extensions, current work,
prior Kit releases and previously dispatched archives. This release authorizes no database writes.

- Replace only the copied Kit base and record version/digest, path, owner, date and project revision.
- Correct NEW-only assumptions: same-object edits preserve IDs and use guarded UPDATE. New IDs are
  for deliberate clones/new objects. Historical revision retention does not forbid current replacement.
- Plan the full [portable return cycle](../contracts/morph-return-cycle.md). Preserve complete offline
  compendia and original experiments. Native codecs/adapters and semantic conversion are implementation
  dependencies; merely renaming existing candidate fields does not establish conformance.
- Content-only returns contain changed plural complete Morph bodies and required dependencies plus
  receiver-agreed manifest, not a mandatory full application ZIP. Omissions are not deletions.
- Separate documentation adoption, source hot deployment, content admission, database connection,
  native NEW/UPDATE proof and Slice display. Report requirements versus implemented evidence explicitly.

Acknowledgement must include exact adopted version/digest, installed path, preserved supplements,
project revision plus dirty/committed/pushed state, and concrete remaining codec/import/deployment gaps.
All-Morph export and worked native-validated examples are still pending. Documentation release and
adoption do not claim any of the next implementation cycle is complete.
